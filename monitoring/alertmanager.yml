global:
  # No email configuration needed!

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'console-alerts'

receivers:
  - name: 'console-alerts'
    # No actual notification - just visible in Alertmanager UI
